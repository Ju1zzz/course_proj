@model IEnumerable<course_project.Models.Guild>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nameGuild)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.firmGuild)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nameGuild)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.firmGuild)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_guild }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_guild }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_guild })
        </td>
    </tr>
}

</table>
